DB_HOST=db       # Use 'db' for local Docker Compose; use your RDS/Aurora host in prod
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=gochujang
DB_NAME=gochujang
DB_SSL=disable           # Use 'require' or 'verify-full' in production

# Full DSN for migration container
# Format: postgres://user:password@host:port/dbname?sslmode=disable
DB_DSN=postgres://postgres:devpass@localhost:5432/gochujang?sslmode=disable

# App config (optional for your Go app)
APP_PORT=8080
ENV=local                # or 'production'
